To test this function from local 
  pip install python-lambda-local

To check if the file indentetion is correct or not
  python -m tabnanny yourfile.py
  
  
command to run for executing the script
  python-lambda-local -f lambda_handler calculator.py event.json
